# Environment variables for the API service
# Copy to .env and edit values as needed

# Runtime environment: dev | test | prod
ENVIRONMENT=dev

# Discord OAuth
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret
# In dev, Vite runs on 5173 by default
DISCORD_REDIRECT_URI=http://localhost:5173/auth/callback

# Session token config
JWT_SECRET_KEY=change-me-in-prod
JWT_EXPIRATION_HOURS=24

# Observability (optional, prod)
LOGFIRE_TOKEN=

# BitCraft game data path required for seeding/FTS
# Example (Docker compose sets this to /app/bitcraft-gamedata/server/region)
BITCRAFT_GAMEDATA_DIR=/absolute/path/to/BitCraft_GameData/server/region
